name: Image scanner

on:
  workflow_call:
    secrets:                 # ✅ ADD THIS
      DOCKERHUB_TOKEN:
        required: true

jobs:
  image-scanner:
    runs-on: ubuntu-latest

    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Trivy scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: manmeeetkaur/github-actions-app:latest   # ✅ WHAT TO SCAN
          severity: CRITICAL
          exit-code: '1'